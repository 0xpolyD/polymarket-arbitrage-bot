version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped

  claimer:
    build:
      context: ./auto_claimer
      dockerfile: Dockerfile
    env_file:
      - ./auto_claimer/.env
    restart: unless-stopped
